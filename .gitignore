# Binaries and project files / Makefiles
build/

# clangd compile flags (only used by the clangd neovim language server)
compile_flags.txt

# premake binaries
tools/premake/*

# OpenNeighborhood generated files
imgui.ini
OpenNeighborhood.ini
